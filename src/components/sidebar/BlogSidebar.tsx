import SidebarContainer from '@/containers/SidebarContainer';

const BlogSidebar = () => {
  return (
    <SidebarContainer>
      <div>search bar</div>
      <nav>navigation</nav>
    </SidebarContainer>
  );
};

export default BlogSidebar;
